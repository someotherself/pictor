[package]
name = "pictor-write"
version = "0.1.0"
edition = "2021"

[features]
default = ["flate2-compress"]

flate2-compress = ["dep:flate2"]
stb-compress = []

[dependencies]
flate2 = { version = "1.1.9", optional = true }
pictor-core = { version = "0.1.0", path = "../pictor-core" }
